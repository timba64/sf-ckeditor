<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
			<header>
				<div class="collapse navbar-collapse" id="navGuestbook">
					<ul class="navbar-nav ml-auto">
							<li class="nav-item"><a class="nav-link" href="{{path('homepage')}}">Home</a>
							<li class="nav-item"><a class="nav-link" href="{{path('about')}}">about</a>
					</ul>
				</div>
			</header>
			<main class="mt-5">
				<div class="container">
							{% block body %}{% endblock %}
					</div>
			</main>
    </body>
</html>
